import { School } from "./School";


export class CoachDetail {
	constructor (
		coachDetailId?: number,
		cellPhone?: string,
		createDate?: Date,
		name?: string,
		schoolId?: number,
		telephone?: string,
		email?: string,
		school?: School ) { };
}
